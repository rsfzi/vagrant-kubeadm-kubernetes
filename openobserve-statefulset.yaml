apiVersion: v1
kind: Service
metadata:
  name: openobserve
  namespace: openobserve
spec:
  #clusterIP: None
  type: NodePort
  selector:
    app: openobserve
  ports:
  - name: http
    port: 5080
    targetPort: 5080
    nodePort: 32080
---
# create statefulset
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: openobserve
  namespace: openobserve
  labels:
    name: openobserve
spec:
  serviceName: openobserve
  replicas: 1
  selector:
    matchLabels:
      name: openobserve
      app: openobserve
  template:
    metadata:
      labels:
        name: openobserve
        app: openobserve
    spec:
      securityContext:
        fsGroup: 2000
        runAsUser: 10000
        runAsGroup: 3000
        runAsNonRoot: true
      # terminationGracePeriodSeconds: 0
      containers:
        - name: openobserve
          image: public.ecr.aws/zinclabs/openobserve:v0.14.6-rc3
          env:
            - name: ZO_ROOT_USER_EMAIL
              value: root@example.com
            - name: ZO_ROOT_USER_PASSWORD
              value: admin
            - name: ZO_DATA_DIR
              value: /data
          # command: ["/bin/bash", "-c", "while true; do sleep 1; done"]
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 4096m
              memory: 2048Mi
            requests:
              cpu: 256m
              memory: 50Mi
          ports:
            - containerPort: 5080
              name: http
          volumeMounts:
          - name: openobserve-data
            mountPath: /data
      volumes:
        - name: openobserve-data
          persistentVolumeClaim:
            claimName: openobserve-data-pvc
      tolerations:
      - key: "datanode"
        operator: "Exists"
        effect: "NoExecute"

